{{system_prompt}}

This analysis covers a software migration effort involving both the target environment and the application's source code. The application's modules, classes, functions, packages, services, configurations, and external dependencies are a primary focus of this investigation.

The following is reference material describing RHEL 8 to RHEL 10 upgrade requirements.
Use it to identify specific compatibility issues for each component below.

--- BEGIN RHEL UPGRADE CONTEXT ---
{{additional_context}}
--- END RHEL UPGRADE CONTEXT ---

The following is a summary of the codebase being migrated.

--- BEGIN APPLICATION CONTEXT ---
{{answers[0]}}
--- END APPLICATION CONTEXT ---

Recommend a migration plan for the codebase targeting RHEL 10.
Rank components so that those with the fewest dependencies come first (leaf-first order) to minimize breaking changes.
For each component, explicitly check for the issues listed in the "IMPLICATIONS FOR CODE ANALYSIS" section of the above
context (SHA-1 usage, network-scripts, JBoss EAP, Stratis filesystem, Python/Java version, DBM databases,
weak cryptographic policies, hard-coded OS version checks).
{
    "migration_plan": [
        {
            "rank": <migration step number>,
            "rank_justification": <rationale for migration rank>,
            "name": <component name>,
            "description": <component description>,
            "git_repo": <git repo url associated with the component>,
            "relevant_files": [<file paths explicitly present in the graph data only, do not infer>],
            "migration_complexity": <low | medium | high>,
            "migration_complexity_score": <0-10>,
            "migration_complexity_justification": <rationale for complexity>,
            "rhel10_compatibility_issues": <known or likely compatibility issues with RHEL 10>,
            "rhel10_compatibility_issues_reference_sources": [<titles or identifiers of documents from additional_context that informed this entry, if any>]
        }
    ]
}

{{post_amble}}
